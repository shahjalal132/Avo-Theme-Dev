<?php

if ( file_exists( dirname( __FILE__ ) . '/slider-shortcode.php' ) ) {
    require_once dirname( __FILE__ ) . '/slider-shortcode.php';
}

if ( file_exists( dirname( __FILE__ ) . '/about-shortcode.php' ) ) {
    require_once dirname( __FILE__ ) . '/about-shortcode.php';
}

if ( file_exists( dirname( __FILE__ ) . '/services-shortcode.php' ) ) {
    require_once dirname( __FILE__ ) . '/services-shortcode.php';
}

if ( file_exists( dirname( __FILE__ ) . '/counterup-shortcode.php' ) ) {
    require_once dirname( __FILE__ ) . '/counterup-shortcode.php';
}

if ( file_exists( dirname( __FILE__ ) . '/portfolio-shortcode.php' ) ) {
    require_once dirname( __FILE__ ) . '/portfolio-shortcode.php';
}

if ( file_exists( dirname( __FILE__ ) . '/testimonial-section.php' ) ) {
    require_once dirname( __FILE__ ) . '/testimonial-section.php';
}

if ( file_exists( dirname( __FILE__ ) . '/vc-map.php' ) ) {
    require_once dirname( __FILE__ ) . '/vc-map.php';
}
